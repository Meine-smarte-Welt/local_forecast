{
  "config": {
    "step": {
      "user": {
        "title": "Set up local weather forecast",
        "description": "Select the sensors of your weather station. Pressure and temperature are required; wind bearing noticeably improves the result.\n\nImportant: for pressure, pick the RELATIVE pressure of your gateway (already reduced to sea level). Only enable the switch below if you deliberately picked the absolute pressure.",
        "data": {
          "name": "Name",
          "pressure_sensor": "Air pressure",
          "pressure_is_absolute": "The selected sensor reports absolute pressure",
          "temperature_sensor": "Outdoor temperature",
          "humidity_sensor": "Humidity",
          "wind_bearing_sensor": "Wind bearing",
          "wind_speed_sensor": "Wind speed",
          "rain_rate_sensor": "Rain rate"
        },
        "data_description": {
          "pressure_sensor": "On an Ecowitt gateway (e.g. GW2000) this entity is usually named \"Relative pressure\".",
          "pressure_is_absolute": "Only enable if you deliberately chose absolute pressure. Reduction to sea level then uses the elevation from the options.",
          "wind_bearing_sensor": "In degrees (0-360). The algorithm works without it, but less accurately.",
          "rain_rate_sensor": "In mm/h. Used to show the measured state instead of the forecast while it is actually raining."
        }
      }
    },
    "error": {
      "entity_not_found": "This entity does not exist.",
      "not_numeric": "This entity currently does not report a numeric value."
    },
    "abort": {
      "already_configured": "A forecast for this pressure sensor already exists."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Local weather forecast",
        "data": {
          "trend_hours": "Trend window",
          "update_interval": "Update interval",
          "elevation": "Elevation above sea level"
        },
        "data_description": {
          "trend_hours": "Period used to compute the pressure trend. The classic algorithm is designed around 3 hours.",
          "update_interval": "How often the forecast is recalculated. More often than every few minutes gains nothing, as pressure changes slowly.",
          "elevation": "Only needed when using absolute pressure. Prefilled from your Home Assistant configuration."
        }
      }
    }
  },
  "entity": {
    "weather": {
      "local_forecast": {
        "state_attributes": {
          "zambretti_code": { "name": "Zambretti code" },
          "zambretti_text": { "name": "Forecast text" },
          "pressure_trend": { "name": "Pressure trend" },
          "sea_level_pressure": { "name": "Sea level pressure" },
          "sample_count": { "name": "Samples in trend window" }
        }
      }
    }
  }
}
